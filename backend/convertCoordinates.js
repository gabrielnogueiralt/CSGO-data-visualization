export function CalculatePointToResolutionXCache(x) {
  const resX = 435;
  x = resX * ((x - -2031) / (3752 - -2031));
  return x;
}

export function CalculatePointToResolutionYCache(y) {
  const resY = 450;
  y = resY * ((y - -2240) / (3187 - -2240));
  return y;
}

export function CalculatePointToResolutionXInferno(x) {
  const resX = 435;
  x = resX * ((x - -1960) / (2797 - -1960));
  return x;
}

export function CalculatePointToResolutionYInferno(y) {
  const resY = 450;
  y = resY * ((y - -1062) / (3800 - -1062));
  return y;
}

export function CalculatePointToResolutionXMirage(x) {
  const resX = 435;
  x = resX * ((x - -3217) / (1912 - -3217));
  return x;
}

export function CalculatePointToResolutionYMirage(y) {
  const resY = 450;
  y = resY * ((y - -3401) / (1682 - -3401));
  return y;
}
